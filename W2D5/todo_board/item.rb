class Item
    def self.valid_date?(str)
        format = str.split('-')
        return false if format[0].to_i < 0 || format[0].to_i > 2022
        return false if 
    def initialize
        
    end
end
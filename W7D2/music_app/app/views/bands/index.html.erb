<h1>Bands#index</h1>
<p>Find me in app/views/bands/index.html.erb</p>
<ul>
    <% @bands.each do |band| %>
        <li><%=link_to "Band: #{band.name}", band_url(band) %></li>
    <% end %>
</ul>

<%= link_to "Add Band", new_band_url %>
